import pandas as pd


# def join_reports(fct_df, dim_df):

    # orders_and_users_df = pd.merge(fct_orders_df, dim_users_df, on='user_id', how='left')

    # use SQL to select and join tables from silver layer tables
